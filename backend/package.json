{
  "name": "prepai-backend",
  "version": "1.0.0",
  "private": true,
  "description": "Express backend for PrepAI",
  "scripts": {
    "dev": "node --watch src/server.js",
    "start": "node src/server.js",
    "migrate": "node src/scripts/migrate.js",
    "test": "node --test"
  },
  "dependencies": {
    "dotenv": "^16.6.1",
    "express": "^5.1.0",
    "pg": "^8.8.0"
  },
  "engines": {
    "node": ">=20.0.0 <=24.x.x",
    "npm": ">=10.0.0"
  }
}
